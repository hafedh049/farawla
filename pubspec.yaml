name: farawla
description: Revision platform.

publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter 

  font_awesome_flutter: ^10.6.0
  flutter_animate: ^4.3.0
  highlight: ^0.7.0
  flutter_highlight: ^0.7.0
  flutter_code_editor: ^0.3.1
  lottie: ^2.7.0
  searchbar_animation: ^0.0.4
  hive: ^2.2.3
  flutter_tilt: ^2.1.2
  path_provider: ^2.1.1
  image_picker: ^1.0.4
  flutter_acrylic: ^1.1.3
  bitsdojo_window: ^0.1.5
  screenshot: ^2.1.0
  uuid: ^4.2.1
  awesome_snackbar_content: ^0.1.3
  anim_search_bar: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  generate: true

  assets:
    - assets/